---
version: "2"
run:
  timeout: 10m
linters:
  enable:
    - whitespace
    - misspell
  settings:
    errcheck:
      exclude-functions:
        - (github.com/hashicorp/terraform-plugin-sdk/v2/helper/schema.*ResourceData).Set
formatters:
  enable:
    - goimports
